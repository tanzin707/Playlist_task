# Server Configuration
PORT=4000
NODE_ENV=development

# Database
DATABASE_URL="file:./dev.db"

# WebSocket
WS_PORT=4000

# Client Configuration (Next.js will use its own env if needed)
NEXT_PUBLIC_API_URL=http://localhost:4000

